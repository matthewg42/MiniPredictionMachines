BUILD_EXTRA_FLAGS += -DDEBUG
BUILD_EXTRA_FLAGS += -DDEBUG_OUTPUT=Serial
BUILD_EXTRA_FLAGS += -DTHINGSPEAK_UPLOAD
BUILD_EXTRA_FLAGS += -DTIMESTREAMS_UPLOAD
BUILD_EXTRA_FLAGS += -DDISPLAY_BATTERY_VOLTAGE
BOARD = nodemcu
UPLOAD_SPEED = 921600

include Makefile.apikeys

include ../esp.mk

